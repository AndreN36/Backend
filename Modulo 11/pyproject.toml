[tool.poetry]
name = "modulo 11"
version = "0.1.0"
description = "Modulo de validaçao API"
authors = ["Andre Niemczyk <andre.lnf3@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12,<4.0"
uvicorn = "^0.42.0"
fastapi = "^0.135.1"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
